---
name: travel-skill
description: Use when the user wants to create a travel guide or itinerary - asks for destination, dates, budget, and preferences, then generates a comprehensive Markdown travel guide + HTML mobile viewer with QR code sharing
---

# Travel Skill

通过对话收集用户偏好，生成中文旅行攻略 Markdown 文档 + HTML 移动端查看器，附带 QR 码方便手机扫码查看。

## 核心原则

- 一问一答，每次只问一个问题
- **用户已提供的信息绝不重复询问** — 每条信息只问一次，即在对话中已出现的（如"我想去东京"）直接从问题列表移除
- 信息越多攻略越精准，但允许用户跳过（提供合理默认值）
- 所有输出使用中文
- 攻略数据结构统一，按层级展示
- 行程节奏从兴趣偏好自动推断（"休闲度假"→悠闲，"户外探险"→紧凑，默认适中）
- 雨天备选方案默认包含，无需询问
- **默认用餐时间：早餐 08:30、午餐 11:30、晚餐 18:00**，根据当天行程可微调 ±30 分钟
- **关键时间节点（发车、入园、表演等）必须高亮标注**

## 参考文档

详细的数据结构、MCP 对照表和输出模板见以下文件，按需加载：

| 文档 | 路径 | 何时加载 |
|------|------|---------|
| 数据结构定义 | `${CLAUDE_SKILL_DIR}/references/data-structure.md` | 攻略生成阶段 |
| MCP 集成策略 | `${CLAUDE_SKILL_DIR}/references/mcp-integration.md` | MCP 准备阶段 |
| 输出模板 | `${CLAUDE_SKILL_DIR}/references/output-template.md` | 输出阶段 |

## MCP 环境准备阶段

**在询问任何旅行信息之前完成 MCP 环境检查，一次性呈现结果。**

### 第一步：检测已有 MCP

检查系统提供的 function list 中 `mcp__` 前缀的工具，按以下类别归类：

| # | 类别 | 常见 MCP 名称模式 | 用途 |
|---|------|-----------------|------|
| 1 | 地图 | `mcp__google-maps` / `mcp__amap` / `mcp__gaode` | 路线规划、距离计算、交通时间 |
| 2 | 火车票 | `mcp__rail` / `mcp__12306` | 车次查询、票价、余票 |
| 3 | 机票 | `mcp__flight` / `mcp__skyscanner` | 航班时刻、价格对比 |
| 4 | 天气 | `mcp__weather` / `mcp__openweather` / `mcp__qweather` | 目的地天气预报 |
| 5 | 汇率 | `mcp__exchange` / `mcp__currency` | 国际旅行货币换算 |

### 第二步：批量呈现结果

一次性告知用户 MCP 就绪情况和缺失项，不逐个询问。

对缺失的 MCP，简述其价值。用户可输入编号选择安装，或回复"跳过"全部使用搜索降级。

示例：
> "MCP 环境检测：✅ 地图、天气 / ⚠️ 火车票、机票、汇率缺失。安装哪些？（输入编号或'跳过'）"

### 第三步：协助安装

用户选择安装时：
1. `WebSearch` 搜索该 MCP 的最新安装文档和 npm 包名
2. 提供安装命令和配置 JSON 片段
3. 不自动执行安装命令，由用户操作完成后确认

## 信息收集阶段

按以下顺序逐一询问，每次一个问题。**如用户在对话中已提供某项信息，跳过该问题。**

| # | 问题 | 选项 |
|---|------|------|
| 1 | **目的地** | 城市或国家（可多个） |
| 2 | **出行日期** | 具体日期或大致时间段 |
| 3 | **预算** | 经济型 / 舒适型 / 豪华型 / 自定义金额 |
| 4 | **出行人数** | 独自 / 情侣 / 家庭 / 朋友团体，是否有老人或儿童 |
| 5 | **兴趣偏好** | 自然风光 / 历史文化 / 美食 / 购物 / 户外探险 / 休闲度假 / 网红打卡 / 演出赛事 |
| 6 | **交通偏好** | 公共交通 / 租车自驾 / 包车 / 步行友好 |
| 7 | **住宿偏好** | 青旅 / 民宿 / 经济酒店 / 高档酒店 / 度假村 |
| 8 | **饮食要求** | 无特别要求 / 素食 / 清真 / 当地特色优先 / 高档餐厅体验 |
| 9 | **其他需求** | 自由文本（提示：特殊日期、无障碍需求、签证情况等） |

## 攻略生成阶段

### 信息搜索

根据用户偏好，按以下优先级获取各类信息：

**MCP 优先 → WebSearch 降级：**

| 类别 | MCP 优先 | 降级方案 | 标注 |
|------|----------|---------|------|
| 地图/路线 | Google Maps / Amap MCP | WebSearch 估算 | ✅ 实时 / 📌 参考 |
| 火车票 | 12306 / Rail API MCP | WebSearch 参考车次 | 同上 |
| 机票 | Flight / Skyscanner MCP | WebSearch 参考航班 | 同上 |
| 天气 | OpenWeather / 和风天气 MCP | WebSearch 历史趋势 | 同上 |
| 汇率 | Exchange Rate MCP | WebSearch 近期汇率 | 同上 |

搜索内容清单：
- 核心景点、开放时间、门票价格、**关键时间节点**（表演时间、换岗时间、最佳入场时间等）
- 景点间距离和推荐路线（地图 MCP 优先）
- 大交通方案：机票/火车票（Flight/Rail MCP 优先）
- 当地特色美食和推荐餐厅：**具体餐厅名称、地址、大众点评评分（0-5.0）、评论数、人均价格、必点菜品、预订方式**
- 住宿区域推荐：**具体酒店名称、地址、大众点评评分（0-5.0）、评论数、人均价格/晚**
- 当地交通卡/通票信息
- 出行期间天气预报（天气 MCP 优先）
- 汇率预算换算（汇率 MCP 优先，国际旅行场景）
- 签证要求（中国护照）
- 当地习俗和注意事项
- 常用当地语言短语
- 紧急联系方式
- 大众点评/携程评分和评论数（餐厅和酒店必须标注）

### 攻略内容

使用 `${CLAUDE_SKILL_DIR}/references/data-structure.md` 定义的数据结构组织信息。

**以下内容默认包含，无需询问用户：**
- **每日三餐** — 每天行程必须包含早餐（🥣 08:30）、午餐（🥢 11:30）、晚餐（🍖 18:00），以独立的带高亮样式行呈现。用餐时间可根据当天行程微调 ±30 分钟（如赶早班车则早餐提前至 08:00），但必须标注具体时间
- **重大事件节点** — 每天 5-8 个带时间标注的关键节点。三餐和重要时间点（发车、入园、表演、返程等）必须使用彩色时间徽章（`.key-time`）突出显示，普通活动使用标准时间轴样式
- **雨天备选方案** — 每天行程附带 1-2 个室内备选活动
- **交通卡/通票** — 当地交通卡信息及购买建议
- **自由时间** — 每天预留 1-2 小时自由探索
- **地图链接** — 景点间路线使用 Google Maps/Amap 短链接
- **餐厅预订提示** — 热门餐厅是否需要提前预订及方式
- **大众点评数据** — 餐厅和酒店必须标注评分（0-5.0）和评论数

**生成后执行合理性校验：**
- 同一天相邻景点间交通时间是否合理
- 全天活动是否过于紧凑（景点数 ≤ 4，总时长 ≤ 10 小时）
- 发现不合理时自动调整

## 图片搜索阶段

与内容生成并行：确定景点和美食列表后立即启动图片搜索，不等内容写完。

**核心原则**：
- **每天至少 1 张配图**，包括最后一天（即使只是购物/返程日），重点景点配 2 张
- 国际图库（Pexels 等）缺乏中国景点实拍照片是客观现实——**所有配图必须加"📷 示意图"标注**，不允许假装是实拍
- 图片 URL 必须通过 `curl -sI` 验证 HTTP 200 可达

### 方案 1：Pexels API（主方案）

```
https://api.pexels.com/v1/search?query={英文关键词}&per_page=5
```

免费额度每月 20000 请求。**优先使用 Pexels**，因其 CDN 连通性最佳。

### 方案 2：Pixabay API（备选）

```
https://pixabay.com/api/?q={英文关键词}&per_page=5
```

Pexels 搜索结果不理想时使用。

### 方案 3：Wikimedia Commons（备选，中国景点专用）

对于中国知名景点（赛里木湖等），尝试搜索：
```
https://commons.wikimedia.org/wiki/Category:{英文景点名}
```
Wikimedia 图片为自由授权，URL 格式为 `https://upload.wikimedia.org/wikipedia/commons/...`。

### 图片验证流程（必须执行）

每张图片在写入 HTML 之前：
1. 使用 `curl -sI --max-time 5 "<图片URL>"` 验证 HTTP 200
2. 不通过则换下一张备选
3. 写入 HTML 时移除 `onerror` 隐藏（宁可显示裂图让用户知道，也比静默失败好）

### 搜索范围

- 每个核心景点配 1 张（重点景点 2 张）
- 每天行程至少 1 张图（包括最后一天）
- 美食区配 4 张不同菜品

### 示意图标注规范

每张 `<img>` 后必须紧跟 caption：
```html
<img class="ai" src="已验证URL" alt="描述" loading="lazy">
<small style="display:block;color:var(--light);margin-top:4px">📷 示意图 · {具体描述，关联真实景点特征}</small>
```

## 输出阶段

### 检测前端工具

在生成输出之前，检查系统提供的 skill 列表和 MCP 工具中是否有前端相关工具：

- `frontend-design` skill — 优先使用，可生成高质量 Markdown + HTML
- `mcp__ide__executeCode` — 可用于辅助渲染和预览

### 方案 A：有前端插件（优先）

如果 `frontend-design` skill 可用，使用它生成两个文件：

1. **`<目的地>-旅游攻略.md`** — Markdown 主文档
2. **`<目的地>-旅游攻略.html`** — HTML 移动端查看器（含 QR 码）

传递给前端插件的信息：
- 攻略全部数据（概要、每日行程、住宿、预算、实用信息、图片 URL 等）
- 设计约束：旅行主题暖色调（#e8724a 主色），移动端优先响应式
- HTML 必须包含：
  - 目录导航（TOC section）
  - 行程日卡片使用 `<details>/<summary>` 折叠，默认 Day 1 展开
  - 时间轴统一使用左边框括号卡片（`.ds-r`），**不使用**竖线时间轴（`.tl`）
  - 时间节点分类：`.ds-r.meal`（用餐，粘土色）、`.ds-r.transit`（交通，蓝色）、`.ds-r`（活动，默认）、`.ds-r.free`（自由时间，金色）
  - 关键时间徽章（`.kt`）
  - 右下角固定"回到顶部"按钮（`#btt`，滚动 >300px 出现）
  - 餐厅和酒店名称附带可点击的 Amap 搜索链接（`target="_blank"`）
  - 地图链接使用 URL 编码格式：`from%5Bname%5D=` 非 `from[name]=`
  - QR 码仅用户明确需要分享功能时包含，默认省略
  - 正文字号 ≥17px，主容器 ≥900px
- `${CLAUDE_SKILL_DIR}/references/output-template.md` 中的模板结构和 CSS 变量作为参考基线

### 方案 B：无前端插件（降级）

如果前端插件不可用，使用 `${CLAUDE_SKILL_DIR}/references/output-template.md` 中的模板手动生成两个文件，格式与方案 A 一致。

### 校验清单

输出前确认：
- [ ] 每天包含早餐（08:30）、午餐（11:30）、晚餐（18:00），含具体餐厅、人均、评分
- [ ] 每天包含 5-8 个重大事件节点（含三餐 + 关键活动），统一使用左边框卡片（`.ds-r`）呈现
- [ ] 每天至少 1 张配图，包括最后一天；重点景点配 2 张
- [ ] 所有图片 URL 经过 `curl -sI` 验证 HTTP 200
- [ ] 所有配图后紧跟 `📷 示意图 · 具体描述` caption
- [ ] 餐饮含具体餐厅名、地址、大众点评评分（0-5.0）、评论数
- [ ] 所有餐厅和酒店名称附带可点击的 Amap POI 搜索链接（`target="_blank"`）
- [ ] 住宿含具体酒店名、人均价格/晚、大众点评评分（0-5.0）、评论数、地址
- [ ] 每天行程包含上午/下午/晚上/自由时间
- [ ] 每天包含雨天备选方案
- [ ] 交通卡/通票信息已包含
- [ ] 地图链接使用 `%5Bname%5D` URL 编码格式，非 `[name]=`
- [ ] 餐厅有预订提示
- [ ] 预算表覆盖所有类别
- [ ] 每条数据标注来源类型（✅ 实时 / 📌 参考）
- [ ] 行程日卡片使用 `<details>/<summary>` 折叠，默认 Day 1 展开
- [ ] 右下角"回到顶部"按钮（`#btt`）正常工作
- [ ] `.md` 文档格式完整，表格对齐
- [ ] `.html` 移动端适配（viewport + 响应式 CSS），正文字号 ≥17px

## QR 码与手机分享阶段（可选）

**仅在用户明确需要分享功能时执行此阶段**，默认攻略不包含 QR 码。

两个文件生成完成后，执行以下步骤让用户可扫码在手机上查看 HTML 攻略：

### 第一步：启动本地 HTTP 服务

在攻略文件所在目录启动 Python HTTP 服务（后台运行）：

```bash
cd <输出目录> && python3 -m http.server 8899 &
```

### 第二步：获取本机局域网 IP

```bash
hostname -I | awk '{print $1}'
```

### 第三步：生成并展示 QR 码

QR 码指向 HTML 查看器（移动端友好渲染）：

**方案 A — 下载 QR 图片（推荐）：**

```bash
python3 -c "
import urllib.request
url = 'http://<IP>:8899/<目的地>-旅游攻略.html'
api = f'https://api.qrserver.com/v1/create-qr-code/?size=300x300&data={urllib.request.quote(url)}'
urllib.request.urlretrieve(api, '/tmp/travel_qr.png')
print(f'QR 码已保存: /tmp/travel_qr.png')
print(f'手机扫码地址: {url}')
"
```

**方案 B — 终端 ASCII QR 码：**

使用 `curl "qrenco.de/<URL编码后的地址>"` 在终端直接显示 ASCII 二维码。

**方案 C — HTML 内嵌自生成（已默认包含）：**

HTML 模板已内嵌 qrcodejs 库，页面在浏览器中打开后自动在底部生成当前页 URL 的二维码。

### 第四步：引导用户分享

告知用户：
1. 确保手机和电脑连接同一 Wi-Fi
2. 使用手机相机扫描 QR 码图片（或 HTML 页面底部的 QR 码）
3. 手机浏览器打开 HTML 攻略后，可「添加到主屏幕」实现离线查看
4. `.md` 文档可用 Markdown 阅读器或文本编辑器查看编辑
5. 如需停止服务：`pkill -f "python3 -m http.server 8899"`
